# Runtime deps for the /api serverless function.
# Vercel installs this file automatically for the Python runtime.
#
# Deliberately small. BM25 and the vector maths are implemented in agent/rag using only
# the standard library, because the corpus is ~56 chunks and a numeric stack would cost
# more in cold-start time and bundle size than it saves in compute. When the corpus
# outgrows that, the answer is a hosted vector store, not numpy.
fastapi==0.115.6
pydantic==2.10.4
openai==1.59.6
httpx==0.28.1
python-dotenv==1.0.1
pyyaml==6.0.2
